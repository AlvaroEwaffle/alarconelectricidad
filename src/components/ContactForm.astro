---
// No props needed for this component
---

<section id="contacto" class="py-20 relative" 
         style="background-image: linear-gradient(rgba(255, 255, 255, 0.95), rgba(255, 255, 255, 0.9)), url('/FOTO6.jpg'); background-size: cover; background-position: center; background-attachment: fixed;">
  <!-- Overlay for better text readability -->
  <div class="absolute inset-0 bg-white/20"></div>
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-main mb-6">
        Contacta con nosotros
      </h2>
      <p class="text-xl text-main/80 max-w-3xl mx-auto">
        ¿Tienes un proyecto eléctrico en mente? Te ayudamos a hacerlo realidad
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
      <!-- Contact Information -->
      <div class="space-y-8">
        <div>
          <h3 class="text-2xl font-bold text-main mb-6">Información de Contacto</h3>
          <div class="space-y-6">
            <!-- WhatsApp -->
            <div class="flex items-start space-x-4">
              <div class="w-12 h-12 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-white text-xl">📱</span>
              </div>
              <div>
                <h4 class="font-semibold text-main mb-1">WhatsApp</h4>
                <a href="https://wa.me/56937418142" target="_blank" 
                   class="text-hover hover:text-red-600 transition-colors">
                  +56 9 3741 8142
                </a>
                <p class="text-main/70 text-sm">Respuesta inmediata</p>
              </div>
            </div>

            <!-- Email -->
            <div class="flex items-start space-x-4">
              <div class="w-12 h-12 bg-highlight rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-main text-xl">📧</span>
              </div>
              <div>
                <h4 class="font-semibold text-main mb-1">Email</h4>
                <a href="mailto:alarcon1976suazo@gmail.com" 
                   class="text-hover hover:text-red-600 transition-colors">
                  alarcon1976suazo@gmail.com
                </a>
                <p class="text-main/70 text-sm">Respuesta en 24 horas</p>
              </div>
            </div>

            <!-- Location -->
            <div class="flex items-start space-x-4">
              <div class="w-12 h-12 bg-details rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-main text-xl">📍</span>
              </div>
              <div>
                <h4 class="font-semibold text-main mb-1">Ubicación</h4>
                <p class="text-main/80">Talca, Región del Maule</p>
                <p class="text-main/70 text-sm">Servicio en toda la región</p>
              </div>
            </div>

            <!-- Hours -->
            <div class="flex items-start space-x-4">
              <div class="w-12 h-12 bg-main rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-white text-xl">🕒</span>
              </div>
              <div>
                <h4 class="font-semibold text-main mb-1">Horarios de Atención</h4>
                <p class="text-main/80">Lunes a Viernes: 8:00 - 18:00</p>
                <p class="text-main/80">Sábados: 8:00 - 14:00</p>
                <p class="text-main/70 text-sm">Emergencias 24/7</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Quick Contact Buttons -->
        <div class="bg-soft rounded-2xl p-6">
          <h4 class="font-semibold text-main mb-4">Contacto Rápido</h4>
          <div class="space-y-3">
            <a href="https://wa.me/56937418142?text=Hola%20Francisco,%20necesito%20una%20cotización%20para%20un%20proyecto%20eléctrico" 
               target="_blank" 
               class="block w-full bg-green-500 text-white py-3 px-4 rounded-xl text-center font-semibold hover:bg-green-600 transition-colors">
              💬 WhatsApp - Cotización
            </a>
            <a href="https://wa.me/56937418142?text=Hola%20Francisco,%20necesito%20una%20evaluación%20gratuita" 
               target="_blank" 
               class="block w-full bg-hover text-white py-3 px-4 rounded-xl text-center font-semibold hover:bg-red-600 transition-colors">
              📋 WhatsApp - Evaluación Gratuita
            </a>
          </div>
        </div>
      </div>

      <!-- Contact Form -->
      <div class="bg-soft rounded-2xl p-8">
        <h3 class="text-2xl font-bold text-main mb-6">Envíanos un mensaje</h3>
        <form id="contact-form" class="space-y-6">
          <div>
            <label for="name" class="block text-sm font-medium text-main mb-2">Nombre completo *</label>
            <input 
              type="text" 
              id="name" 
              name="name" 
              required 
              class="w-full px-4 py-3 rounded-xl border border-soft focus:ring-2 focus:ring-hover focus:border-transparent transition-all"
              placeholder="Tu nombre completo"
            >
          </div>

          <div>
            <label for="email" class="block text-sm font-medium text-main mb-2">Email *</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              required 
              class="w-full px-4 py-3 rounded-xl border border-soft focus:ring-2 focus:ring-hover focus:border-transparent transition-all"
              placeholder="tu@email.com"
            >
          </div>

          <div>
            <label for="phone" class="block text-sm font-medium text-main mb-2">Teléfono</label>
            <input 
              type="tel" 
              id="phone" 
              name="phone" 
              class="w-full px-4 py-3 rounded-xl border border-soft focus:ring-2 focus:ring-hover focus:border-transparent transition-all"
              placeholder="+56 9 1234 5678"
            >
          </div>

          <div>
            <label for="service" class="block text-sm font-medium text-main mb-2">Tipo de servicio</label>
            <select 
              id="service" 
              name="service" 
              class="w-full px-4 py-3 rounded-xl border border-soft focus:ring-2 focus:ring-hover focus:border-transparent transition-all"
            >
              <option value="">Selecciona un servicio</option>
              <option value="instalacion-nueva">Instalación eléctrica nueva</option>
              <option value="reparacion">Reparación de fallas</option>
              <option value="mantenimiento">Mantenimiento preventivo</option>
              <option value="luminarias">Instalación de luminarias</option>
              <option value="climatizacion">Sistemas de climatización</option>
              <option value="otro">Otro</option>
            </select>
          </div>

          <div>
            <label for="message" class="block text-sm font-medium text-main mb-2">Mensaje *</label>
            <textarea 
              id="message" 
              name="message" 
              rows="4" 
              required 
              class="w-full px-4 py-3 rounded-xl border border-soft focus:ring-2 focus:ring-hover focus:border-transparent transition-all resize-none"
              placeholder="Cuéntanos sobre tu proyecto eléctrico..."
            ></textarea>
          </div>

          <button 
            type="submit" 
            class="w-full bg-hover text-white py-4 px-6 rounded-xl font-semibold text-lg hover:bg-red-600 transition-all duration-300 transform hover:scale-105 shadow-lg"
          >
            📤 Enviar mensaje
          </button>
        </form>

        <div id="form-success" class="hidden mt-4 p-4 bg-green-100 border border-green-400 text-green-700 rounded-xl">
          ¡Mensaje enviado correctamente! Te contactaremos pronto.
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('contact-form');
    const successMessage = document.getElementById('form-success');

    form.addEventListener('submit', (e) => {
      e.preventDefault();
      
      // Get form data
      const formData = new FormData(form);
      const data = Object.fromEntries(formData);
      
      // Create WhatsApp message
      const message = `Hola Francisco, me contacto desde tu página web:

Nombre: ${data.name}
Email: ${data.email}
Teléfono: ${data.phone || 'No proporcionado'}
Servicio: ${data.service || 'No especificado'}

Mensaje: ${data.message}

¡Espero tu respuesta!`;
      
      // Encode message for URL
      const encodedMessage = encodeURIComponent(message);
      
      // Open WhatsApp
      window.open(`https://wa.me/56937418142?text=${encodedMessage}`, '_blank');
      
      // Show success message
      successMessage.classList.remove('hidden');
      form.reset();
      
      // Hide success message after 5 seconds
      setTimeout(() => {
        successMessage.classList.add('hidden');
      }, 5000);
    });
  });
</script>
